# 一、路由和导航

Flutter路由和导航的概览，<https://docs.flutter.cn/ui/navigation>
Flutter 中文社区教程，<https://docs.flutter.cn/community/tutorials> 👍
deep linking，<https://docs.flutter.cn/ui/navigation/deep-linking>
学习 Flutter 的新导航和路由系统，<https://blog.flutter.dev/learning-flutters-new-navigation-and-routing-system-7c9068155ade> & <https://mp.weixin.qq.com/s/zGpzJahDSTZDhWqYmkzi5g>

Flutter 提供了一个完整的系统，用于在屏幕之间导航 和 处理 Deep linking。
  * Navigator，适用于没有复杂 deep linking 的小型 App；
  * Router，在有特定 deep linking 和 导航 要求的 App 中使用；

Flutter 有一个命令式路由机制，即 Navigator 组件，还有一个更为惯用的声明式路由机制（类似于 widget 中使用的 build 方法），即 Router。
这两个系统可以一起使用（事实上，声明式系统是使用命令式系统构建的）。


# 二、Navigator 组件
<https://api.flutter.dev/flutter/widgets/Navigator-class.html>

Navigator，是一个管理 Route 对象栈的组件。并提供两种管理 Route 栈的方式：
  - 命令式 API，路由通过 `命名路由` 或 `匿名路由（基本路由）` 被 push 到 Navigator 的堆栈中，然后从堆栈中 pop。
  - 声明式 API `Navigator.pages`，它是 `Navigator 2.0` 新增的 API。
  
Route，由 Navigator 管理的对象，代表一个屏幕。
Route 是一个抽象类，最终实现类为：CupertinoPageRoute、MaterialPageRoute、PageRouteBuilder。

MaterialApp 和 CupertinoApp 底层已经使用了 Navigator，MaterialApp 的 home 会作为路由栈的栈底路由。
Navigator 命令式 API 主要如下所示：
  - `Navigator.of()`，获取 Navigator 实例。
  - `Navigator.push()`，添加新的页面到路由栈，并显示。
  - `Navigator.pop()`，移除当前页面到路由栈，返回上一个页面。

路由参数传递可参考：<https://docs.flutter.cn/cookbook/navigation/passing-data/>


