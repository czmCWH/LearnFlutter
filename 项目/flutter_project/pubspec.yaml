name: flutter_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  carousel_slider: ^4.2.1   # 轮播组件
  flutter:
    sdk: flutter
  flutter_hi_cache: ^0.0.1  # preference 本地存储
  flutter_staggered_grid_view: ^0.7.0   # 处理瀑布流布局
  http: ^1.2.2              # 网络请求
  transparent_image: ^2.0.1   # 处理图片
  underline_indicator: ^0.0.4   # 处理 TabBar 下划线
  url_launcher: ^6.3.0      # 访问网页URL

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - images/